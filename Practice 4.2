DROP TABLE IF EXISTS StudentEnrollments;

CREATE TABLE StudentEnrollments (
    enrollment_id INT PRIMARY KEY AUTO_INCREMENT,
    student_name VARCHAR(100) NOT NULL,
    course_id VARCHAR(10) NOT NULL,
    enrollment_date DATE NOT NULL,
    CONSTRAINT uq_student_course UNIQUE (student_name, course_id)
);

INSERT INTO StudentEnrollments (student_name, course_id, enrollment_date) VALUES 
('Ashish', 'CSE101', '2024-07-01'),
('Smaran', 'CSE102', '2024-07-01'),
('Vaibhav', 'CSE101', '2024-07-01');

-- To avoid duplicate error, check before insert or use INSERT IGNORE / ON DUPLICATE KEY UPDATE

-- Example: Insert only if not exists (MySQL 8.0+)
INSERT INTO StudentEnrollments (student_name, course_id, enrollment_date)
SELECT * FROM (SELECT 'Ashish', 'CSE101', '2024-07-01') AS tmp
WHERE NOT EXISTS (
    SELECT 1 FROM StudentEnrollments WHERE student_name = 'Ashish' AND course_id = 'CSE101'
) LIMIT 1;

-- Transaction to update enrollment_date safely with row locking
START TRANSACTION;
SELECT * FROM StudentEnrollments WHERE student_name = 'Ashish' AND course_id = 'CSE101' FOR UPDATE;
UPDATE StudentEnrollments SET enrollment_date = '2024-07-20' WHERE student_name = 'Ashish' AND course_id = 'CSE101';
COMMIT;

-- Final select to view table
SELECT * FROM StudentEnrollments;

